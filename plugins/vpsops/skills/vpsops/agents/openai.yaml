interface:
  display_name: "VPSOps"
  short_description: "Secure VPS, client, relay, monitoring, and maintenance"
  default_prompt: "Use $vpsops to plan, secure, deploy, configure clients, monitor, or audit my VPS setup."

policy:
  allow_implicit_invocation: false
